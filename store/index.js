export const actions = {
    nuxtServerInit({ commit }, { req }) {
      // Executa uma ação ao inicializar o estado do Vuex no lado do servidor
    }
  }
  
  export const plugins = [
    // Adicione plugins do Vuex aqui
  ]